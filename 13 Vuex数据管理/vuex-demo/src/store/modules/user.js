const state = {
  userInfo: {
    name: 'zs',
    age: 18
  },
  score: 80
}
const mutation = {}
const actions = {}
const getters = {}
// 导出
export default {
  state,
  mutation,
  actions,
  getters
}
